The HTML document type declaration, or DOCTYPE, is the first line of code in every HTML or XHTML document. 
It's an instruction to the web browser about what version of HTML the page is written in. 
This ensures that the web page is parsed the same way by different web browsers